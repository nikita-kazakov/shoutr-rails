%h1 Dashboard

=form_for @shout do |form|
  =form.text_field :body, placeholder: "shout here!", required: true
  =form.submit "Submit"

-#Commented out because we're going to render this in partial.
-#-@shouts.each do|shout|
-#  %div
-#    =shout.user.username
-#    =shout.body
-#    ="#{time_ago_in_words(shout.created_at)} ago"

=render @shouts
